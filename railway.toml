[build]
# Use Dockerfile for build
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Start command (already defined in Dockerfile CMD)
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
